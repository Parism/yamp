package logger

func init() {
	//CheckKeyExists("requests")
	/*
		if exists and is for today continue
		if exists but is for yesterday evict it and create a new one
		if it does not exist create it
	*/
	/*
		db read counter
		db write counter
		request served counter
	*/
}
