package models

/*
Groupld struct
is used to represent
the lambdas or deltas at the template level
we use a single struct since we want to represent common fields
*/
type Groupld struct {
	ID   int
	Name string
}
